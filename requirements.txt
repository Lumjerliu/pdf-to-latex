# PDF to LaTeX Converter - Dependencies

# Web Framework
flask>=2.3.0
werkzeug>=2.3.0

# OCR Backends (install at least one)
# Nougat - Best for academic papers with math/tables
nougat-ocr>=0.1.0

# Texify - Excellent for math-heavy documents
# texify>=0.1.0

# pix2tex - Specialized for math equations
# pix2tex>=0.1.0
# pdf2image>=1.16.0

# Core ML dependencies (usually installed with backends)
torch>=2.0.0
transformers>=4.30.0
Pillow>=9.0.0

# PDF processing
pypdf>=3.0.0
